{
  "name": "fullscreen-stream-app",
  "version": "1.0.0",
  "description": "Full page YouTube Live Stream Phone App",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/",
    "cap:sync": "cap sync"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
